<div class="grid">

    <div class="grid__item large--one-half push--large--one-quarter">
        <h1 class="large--text-center space-bottom-x1">{{ page.title }}</h1>

        <div class="rte">
            {{ page.content }}
        </div>

        <div class="text-center spaced space-bottom-x2">
            <a class="btn btn--large no--underline" href="{{ shop.url }}/collections/all">Shop now</a>
        </div>
    </div>

</div>
